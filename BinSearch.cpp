#include "stdio.h"
#include "conio.h"

int main()
{
	int k = 0,array[];
	for (int b = n / 2; b >= 1; b /= 2) {
		while (k + b < n && array[k + b] <= x) k += b;
	}
	if (array[k] == x) {
		cout<<k
	}	return 0;
}